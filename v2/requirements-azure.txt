# Azure cloud-adapter lock (P2). Only installed into images/processes that use
# the Azure backends (Blob object storage, Service Bus queue). Kept out of the
# default local images so the local path never pays for the Azure SDK.
azure-storage-blob==12.30.0
azure-servicebus==7.14.3
azure-identity==1.25.3
azure-core==1.41.0
cryptography==50.0.0
isodate==0.7.2
msal==1.37.0
msal-extensions==1.3.1
PyJWT==2.13.0
cffi==2.1.1
pycparser==3.0
requests==2.34.2
certifi==2026.7.22
urllib3==2.7.0
charset-normalizer==3.5.1
idna==3.19
six==1.17.0
