# Azure Monitor's supported OpenTelemetry distribution. The generated lock in
# requirements-observability.txt is installed only in production API/worker
# images; local tests keep observability disabled and need no Azure service.
azure-monitor-opentelemetry==1.8.9
